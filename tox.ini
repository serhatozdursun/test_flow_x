[tox]
envlist = py39
skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
    jsonschema
    pytest-mock
commands =
    pytest --cov=helper --cov=postman --cov=jmx --cov-report=xml --cov-config=tox.ini --cov-branch

[coverage:run]
relative_files = True
source = test_flow_x/
branch = True
